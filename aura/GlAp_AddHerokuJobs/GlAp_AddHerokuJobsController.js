({
	doInit : function(component, event, helper) {
        helper.doInit(component, event, helper);
	},
    
    handleTab : function(component, event, helper) {
        helper.handleTab(component, event, helper);
	},
    
    handleSave : function(component, event, helper) {
        helper.handleSave(component, event, helper);
	},
    
    handleRecordBy : function(component, event, helper){
        helper.handleRecordBy(component, event, helper);
    },
    
    handleCancel : function(component, event, helper){
        helper.handleCancel(component, event, helper);
    },
    
    handleDaysOfWeek : function(component, event, helper){
        helper.handleDaysOfWeek(component, event, helper);
    }
})