({
	doInit: function(component, event, helper) {
		helper.doInit(component);
	},
	
	handleTab: function(component, event, helper) {
		helper.handleTab(component);
	},
	
	selectDayOfWeek: function(component, event, helper) {
		helper.selectDayOfWeek(component, event);
	},
	
	startSchedule: function(component, event, helper) {
		helper.startSchedule(component);
	},

	stopSchedule: function(component, event, helper) {
		helper.stopSchedule(component);
	},
})