({
	//Show Manage Contract Screen.
    showManageSupplyPointComponent: function(component, event, helper) {
        helper.showManageSupplyPointComponent(component);
	},
    //Close Manage Contract Screen.
    doCancel: function(component, event, helper) {
        helper.doCancel(component);
	},
    // Call wizard screen for transfer service scenario.
    doSave : function(component, event, helper) {
        helper.doSave(component);
	},
})