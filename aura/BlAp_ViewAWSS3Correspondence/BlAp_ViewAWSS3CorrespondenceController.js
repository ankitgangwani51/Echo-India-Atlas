({
    //Initialize lightning constructor.
    doInit : function(component, event, helper) {
        helper.doInit(component, event, helper);
    },
    //View S3 Document
    ViewDocument: function(component, event, helper) {
        helper.ViewDocument(component, event, helper);
    },
   
})