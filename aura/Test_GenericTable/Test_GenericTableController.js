({
	handleRowClickEvent: function(component, event, helper) {
        helper.handleRowClickEvent(component, event, helper);
    },
    
    // Row Select Event method
    handleRowSelectEvent: function(component,event,helper){
    	return helper.handleRowSelectEvent(component, event, helper);
    },
    
    doEdit : function(component,event,helper){
		helper.doEdit(component, event, helper);
	},
    
    handleRecordsList : function(component,event,helper){
        helper.handleRecordsList(component, event, helper);
    },
    
    handleNext : function(component,event,helper){
        helper.handleNext(component, event, helper);
    },
    
    handleFirst : function(component,event,helper){
        helper.handleFirst(component, event, helper);
    },
    
    handleLast : function(component,event,helper){
        helper.handleLast(component, event, helper);
    },
    
    handlePrevious : function(component,event,helper){
        helper.handlePrevious(component, event, helper);
    }
})