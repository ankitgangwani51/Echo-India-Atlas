public class GlUt_APIConstantsChannelOpen {
    // Get the prefix and append it to the object and fields api names
    private static String NAMESPACEPREFIX = GlUt_NamespacePrefix.getNamespacePrefix();
    
    // Channel Open Object API Name
    public static String  OBJECTAPINAME = NAMESPACEPREFIX + 'ChannelOpen__c';
    
    public static String  RURALCHANNELAPINAME = NAMESPACEPREFIX + 'RuralChannel__c';
    public static String  STARTDATEAPINAME = NAMESPACEPREFIX + 'StartDate__c';
    public static String  ENDDATEAPINAME = NAMESPACEPREFIX + 'EndDate__c';
}